// Creation of an article command
function createCommand(command) {
    if (0) {

        console.log(`Creating article with types: ${types.join(', ')}`);
        console.log(`Names for each type: ${names.join(', ')}`);
    } else {
        console.error('Invalid format. Please provide article types and names.');
    }
};

module.exports = {
    createCommand
};
